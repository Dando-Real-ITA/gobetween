# Copilot Repository Instructions

- Do not create documentation files (for example, markdown summaries, implementation notes, quick-reference guides, or example config docs) unless explicitly requested by the user.
- Focus changes on code and tests only unless documentation is explicitly requested.
